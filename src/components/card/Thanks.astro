---
import { thanks } from '../../constants/thanks'
---
<div>
  <h3>Thanks for reading!</h3>
  <article>
    <h3>Especials thanks to</h3>
    {thanks.map((thank) => (
    <div>
      <a href={thank.url} target="_blank">- {thank.title}</a>
      <p>{thank.description}</p>
    </div>
    ))}
  </article>
  <span>Made patiently 😅</span>
</div>

<style scoped>
div {
  width: 100%;
  display: flex;
  flex-direction: column;
  background-color: var(--color-background);
  color: var(--primary-color);
  > article {
    width: 100%;
    box-sizing: border-box;
    padding: 1rem;
    text-align: start;
    > h3 {
      margin: 0;
      padding: 0;
      color: #eee;
      font-weight: 600;
    }
    >div {
      > p {
        margin: 0;
        padding: 0;
        color: #eee;
        padding-left: 1rem;
        font-size: .8rem;
        box-sizing: border-box;
      }
    }
  }
}
a {
  font-weight: 600;
  margin: 0;
  padding: 0;
  margin-top: .4rem;
  text-decoration: none;
  color: var(--primary-color);
}

</style>
